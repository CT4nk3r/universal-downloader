name: ci-mobile

on:
  push:
    branches: [main, develop]
    paths:
      - 'apps/mobile/**'
      - '.github/workflows/ci-mobile.yml'
  pull_request:
    paths:
      - 'apps/mobile/**'
      - '.github/workflows/ci-mobile.yml'

concurrency:
  group: ci-mobile-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  ios:
    name: iOS (build + test)
    runs-on: macos-14
    timeout-minutes: 60
    defaults:
      run:
        working-directory: apps/mobile/ios
    env:
      DEVELOPER_DIR: /Applications/Xcode_15.4.app/Contents/Developer
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/mobile/package-lock.json

      - name: Install JS deps
        working-directory: apps/mobile
        run: npm ci

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: apps/mobile/ios

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            apps/mobile/ios/Pods
            ~/Library/Caches/CocoaPods
            ~/.cocoapods
          key: ${{ runner.os }}-pods-${{ hashFiles('apps/mobile/ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Pod install
        run: bundle exec pod install --repo-update

      - name: Xcode build-for-testing
        run: |
          set -o pipefail
          xcodebuild \
            -workspace UniversalDownloader.xcworkspace \
            -scheme UniversalDownloader \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build-for-testing | xcpretty

      - name: Xcode test-without-building
        run: |
          set -o pipefail
          xcodebuild \
            -workspace UniversalDownloader.xcworkspace \
            -scheme UniversalDownloader \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            -derivedDataPath build \
            -resultBundlePath build/TestResults.xcresult \
            CODE_SIGNING_ALLOWED=NO \
            test-without-building | xcpretty

      - name: Upload xcresult
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-xcresult
          path: apps/mobile/ios/build/TestResults.xcresult
          if-no-files-found: warn
          retention-days: 14

  android:
    name: Android (instrumented tests)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: apps/mobile/android
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/mobile/package-lock.json

      - name: Install JS deps
        working-directory: apps/mobile
        run: npm ci

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('apps/mobile/android/**/*.gradle*', 'apps/mobile/android/**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Assemble debug + androidTest
        run: ./gradlew :app:assembleDebug :app:assembleAndroidTest --no-daemon

      - name: Run connected Android tests on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          target: google_apis
          arch: x86_64
          profile: pixel_6
          working-directory: apps/mobile/android
          script: ./gradlew :app:connectedDebugAndroidTest --no-daemon

      - name: Upload Android test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-test-reports
          path: |
            apps/mobile/android/app/build/reports/androidTests/**
            apps/mobile/android/app/build/outputs/androidTest-results/**
          if-no-files-found: warn
          retention-days: 14

  unit:
    name: Unit tests (jest)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/mobile/package-lock.json

      - name: Detect mobile jest project
        id: detect
        run: |
          if [ -f apps/mobile/package.json ] && grep -q '"jest"' apps/mobile/package.json; then
            echo "has_jest=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_jest=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install deps
        if: steps.detect.outputs.has_jest == 'true'
        working-directory: apps/mobile
        run: npm ci

      - name: Run jest
        if: steps.detect.outputs.has_jest == 'true'
        working-directory: apps/mobile
        run: npx jest --selectProjects mobile --ci --reporters=default

      - name: Skip notice
        if: steps.detect.outputs.has_jest != 'true'
        run: echo "No mobile jest project detected; skipping."
