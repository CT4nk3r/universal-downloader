# Test-only dependencies for the J2.1 API integration suite.
# Install with: pip install -r tests/api/requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
httpx>=0.27
freezegun>=1.4
# Required transitively by the FastAPI app under test:
#   fastapi, sse-starlette, sqlmodel, aiosqlite, aiofiles, pydantic,
#   pydantic-settings, structlog
# These are declared by apps/api itself and assumed installed.
